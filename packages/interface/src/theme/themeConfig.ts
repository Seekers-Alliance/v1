import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#1890ff',
    colorText: '#FFFFFF',
  },
  components: {
    Modal: {
      // contentBg: 'transparent',
    },
  },
};

export default theme;
